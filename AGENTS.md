# 项目级协作约定

## 里程碑进度维护

- 项目级进度的唯一统筹文档是 `.project-private/PROJECT_PROGRESS.md`。开始任何阶段工作前必须先读取该文档，确认当前状态、进入条件、工作目标和阶段出口。
- 当阶段开始、完成、阻塞、取消、拆分，或目标、依赖、交付物、验收条件发生实质变化时，必须在同一轮工作中同步更新该进度文档。
- 允许按需增加 `M1.1`、`M1.2`、`M5.1`、`M5.2` 等子阶段。新增子阶段必须归属于既有主里程碑，并在进度文档中写明状态、目标、主要交付、依赖和阶段出口。
- 不得为了插入子阶段而重编号已经开始或完成的主里程碑。主线顺序或范围发生变化时，必须同步更新总览和变更记录，并说明原因及影响。
- 阶段完成必须以阶段出口和验证证据为依据，不能只依据代码编写完成。子阶段全部完成后，也需要单独核验父阶段出口，才能把父阶段标记为 `已完成`。
- `.project-private/` 是一个独立的私有 Git 仓库，保存阶段设计、项目进度、ADR 和内部调查，并受公开仓库的 `.gitignore` 整体排除。涉及这些资料的变更必须在私有仓库中提交和推送，不得使用 `git add -f`、调整公开仓库忽略规则或复制内容到公开仓库的方式泄露，除非用户明确授权。
- 执行 Git 操作前必须确认目标仓库：产品代码使用项目根仓库，私有治理资料使用 `.project-private/` 仓库。不得把两个仓库的提交或远程地址混用。

## 双仓库更新协议

### 资料归属

- 公开仓库只保存可以公开审阅和复用的产品源码、测试、构建配置、公开文档、示例及脱敏后的协作信息。
- 私有仓库保存阶段计划、`PROJECT_PROGRESS.md`、未公开 ADR、威胁分析、内部调查、验收证据和可能暴露实施策略的记录。
- Secrets、真实用户文档、完整 Provider 请求/响应和访问凭据不得提交到任一仓库。
- 无法确定资料是否适合公开时，先放入私有仓库并标记待审，不得自行按公开资料处理。

### 开始工作前

- 同时执行 `git status -sb` 和 `git -C .project-private status -sb`，确认两个仓库的当前分支、未提交变更和目标远程；存在无关改动时不得覆盖、暂存或提交。
- 读取 `.project-private/PROJECT_PROGRESS.md` 和对应 Private GitHub Issue，确认阶段、子阶段、目标、进入条件和验收标准。
- 阶段或范围需要变化时，先更新私有进度基线和 Private Issue，再开始公开仓库实现。
- 公开仓库使用符合项目约定的功能分支和 Pull Request；不得直接绕过 `main` 保护规则。私有仓库可直接更新 `main`，但提交前仍需检查差异和资料归属。

### 同一任务涉及两个仓库时

按以下顺序执行，不得把“代码已经推送”当作整个任务已经完成：

1. 在私有进度文档和对应 Issue 中确认工作目标、子阶段及验收条件；需要时新增 `M1.1`、`M5.2` 等子阶段。
2. 在公开仓库的独立分支实施源码、测试和公开文档变更，并完成相应验证。
3. 将公开变更提交、推送并创建 Pull Request；Commit 信息继续遵循项目规定的单行“英文前缀 + 中文简介”格式。
4. 私有仓库可以引用公开仓库的 PR、Issue、Commit 或 Release 作为实施证据。
5. 除本协作文件为执行边界明确指定的 `.project-private/` 本地入口外，公开源码、文档、Issue、PR 和 Release 不得引用私有仓库 URL、私有 Issue 编号、私有内部路径或复述私有内容；跨仓库证据引用只允许“私有指向公开”的单向引用。
6. 根据验证结果回写 `PROJECT_PROGRESS.md`、Private Issue、Milestone 和标签，再提交并推送私有仓库。
7. 最后复查两个仓库均无遗漏的目标变更；只有公开实现与私有状态记录都同步后，才能报告跨仓库任务完成。

仅修改公开实现且不改变阶段状态、范围或验收结论时，不要求制造无意义的私有提交；但对应 Private Issue 中需要记录的实质进展仍应及时更新。仅修改内部计划时，不得产生空的公开提交。

### 状态同步

- `PROJECT_PROGRESS.md` 是阶段状态的正式基线，Private GitHub Issues/Milestones 是执行跟踪视图，公开 Git 历史是产品实现事实；三者职责不同，不得互相替代。
- `未开始`：Private Issue 保持 Open，并使用 `status:planned`。
- `进行中`：Private Issue 保持 Open，并使用 `status:in-progress`。
- `阻塞`：Private Issue 保持 Open，并使用 `status:blocked`，同时记录阻塞条件和解除条件。
- `已完成`：阶段出口和验证证据通过后，使用 `status:complete` 并关闭 Issue；父阶段所有出口满足后才能关闭 Milestone。
- `已取消`：关闭 Issue，使用 `status:canceled`，并在进度文档中记录原因和影响，不得伪装为已完成。
- 状态、标签或 Milestone 与进度文档冲突时，先核实实际情况，并在同一轮工作中修正所有受影响记录。

### 失败与中断处理

- 公开仓库提交、推送、检查或 PR 创建失败时，不得把私有阶段标记为已完成；保留为 `进行中` 或按实际情况标记为 `阻塞`。
- 公开变更成功但私有进度同步失败时，任务状态为“同步未完成”；应优先恢复私有提交或 GitHub 跟踪，不得隐瞒半同步状态。
- 私有计划更新成功但公开实现失败时，在 Private Issue 中记录失败证据和下一步，不得回滚或改写已共享的公开历史来伪造一致性。
- 未经用户明确授权，不得通过强制推送、删除远程分支、重写已发布历史或改变仓库可见性来修复同步问题。

### 完成前检查

- 分别检查两个仓库的 `git status -sb`、待推送提交和目标远程，确认没有把一方的文件提交到另一方。
- 检查公开仓库的暂存区、提交差异和 `git ls-files`；`.project-private/`、阶段设计或内部证据不得出现。
- 验证公开 PR/Commit 与私有进度记录的状态一致；除 `AGENTS.md` 中经批准的本地边界入口外，公开内容不得包含私有 URL、编号、内部路径、凭据或内部摘要。
- 汇报结果时分别说明公开仓库变更、私有仓库变更、验证结果和任何尚未同步或需要用户决定的事项。

## CodeGraph 索引维护

- 本项目因 mjs 无法建立索引，因及时回退，避免无意义的 CodeGraph 检索。

## 当前实施边界

- M0～M3 已完成；M3.1～M3.5 与第二次父阶段完成审计均已通过，公开 PR #4 已 squash 合并为 `16eb046`。
- M3 完成审计已补齐统一应用/CLI 全流程、编辑后导出的结构/目标文本/保护项保真、Validator 结构守卫和路径负例；固定隔离容器全量回归 94/94 通过。
- M4.1～M4.5、ADR-015～ADR-019 与父阶段完成审计已通过；固定 12 文档经 DeepSeek V4 Flash 完成 37 次真实调用/机器候选、用户编辑、Validator、人工审核、批准及普通/Canonical 导出，公开 PR #6 已 squash 合并为 `23679fb`。
- M4 Runner 最终运行层固定为 distroless Node.js 22 Debian 13 digest `sha256:a2723a2817c5b01b8e7b98d567bc8b5a6b0e713e25bfb0a82b6ade4b9db06f50`；完整 Runner 镜像的 Docker Scout 严重/高危扫描为 0/0。依赖构建层和统一测试镜像仍固定既有 Node.js 22.19 基线。
- M4.5 已实现 `google-gemini`、`openai`、`deepseek` 三个固定域名 Adapter、严格响应/用量归一化、预算、文件描述符凭据注入和 Broker→低权限 Pi Runner→控制面执行链；真实验收选择 DeepSeek V4 Flash，其余 Provider 仅完成离线契约验证。
- M5.1～M5.6、ADR-020～ADR-025 与父阶段完成审计已通过；Brave Search、独立 Search/Fetch 网络代理、互联网证据、仅用户可批准的知识提案、批准后事实修订、索引/stale 迭代及恢复闭环完成，公开 PR #7 已 squash 合并为 `8bac532`。
- 独立 M5R 已获实施授权并从公开 PR #8 的 squash merge `1f0beec` 启动；公开实现分支为 `feat-m5r-research`。M5R 在复用 M5 Search/Fetch/Proposal 链的前提下增加用户批准的多轮研究、原子预算、Research Runner、Source/Citation/Claim/ResearchReport 和多来源知识提案。M5 保持已完成，M6 硬依赖 M5R，M5R 父阶段完成前不得启动 M6。
- M5R.1 已完成：schema v20、ResearchRequest/Grant/Run/Query/Source/Citation/Claim/ResearchReport 契约、威胁边界和 90 案例离线评测基线通过；当前推进 M5R.2 离线多轮编排与 Research Runner，继续默认断网且不读取真实 Key。
- M5R 第一版不自动回顾、撤销或重写历史人工译文；知识更新后仅由用户人工触发重新翻译。固定 90 案例以离线 fixture 为主，真实 Search、Fetch、内容服务、研究模型和真实用户文档继续分别门禁。
- 后续阶段的新依赖仍须按对应计划锁定、审查并更新 SBOM；不得提前安装依赖或加入占位实现。
- M4/M5 的既有真实验证授权均已随对应阶段完成而结束。用户允许 M5R.4a 真实 Brave 门禁复用仓库外 `0600` Brave Key 文件，并单独授权最多 10 次公开合成 Search、估算费用硬上限 1 美元、不保存完整原始响应或 Key；该授权只能在 M5R 前置离线子阶段通过后按固定 manifest 执行。Serper、真实公网 Fetch、Tavily Search/Extract、真实研究模型/API 和真实用户文档仍未授权。
- M8 的 Embedding/Rerank 工作是可选、证据驱动且需单独授权的实验，不得因接口已经预留而提前实现。
